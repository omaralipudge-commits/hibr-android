plugins {
    id 'com.android.application'
}

android {
    namespace 'com.hibrmemory.app'
    compileSdk 35

    defaultConfig {
        applicationId 'com.hibrmemory.app'
        minSdk 26
        targetSdk 35
        versionCode 22
        versionName '2.2.0'
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_17
        targetCompatibility JavaVersion.VERSION_17
    }
}
